[
  {
    "id": "column.public.session.revokedAt",
    "label": "Add column \"revokedAt\" to \"session\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "revokedAt",
        "table": "session"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"revokedAt\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "session",
          "revokedAt"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"revokedAt\"",
        "sql": "ALTER TABLE \"public\".\"session\" ADD COLUMN \"revokedAt\" timestamptz"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"revokedAt\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "session",
          "revokedAt"
        ]
      }
    ]
  }
]